[pytest]
testpaths = tests/unit tests/pipeline tests/model
markers =
    unit: Fast, isolated unit tests (no model dependencies)
    slow: Requires loaded models (GPU)
    manual: One-off research tests
asyncio_mode = auto
